node_modules/
dist/
*.sarif
# ...except the checked-in test fixtures. Without these the test suite cannot
# run on a fresh clone, which is exactly what CI does.
!internal/secsuite/testdata/*.sarif
out.json

# Go build output
/secsuite
/secsuite.exe

# Release artifacts, generated at tag time by npm/build.mjs
npm/packages/

# Git Bash crash artifacts
*.stackdump

# internal planning docs - kept local, not published
docs/superpowers/
.lavish/
