node_modules/
dist/
*.sarif
# ...except the checked-in test fixtures. Without these the test suites cannot
# run on a fresh clone, which is exactly what CI does.
!internal/secsuite/testdata/*.sarif
!test/fixtures/*.sarif
out.json

# Go build output
/secsuite
/secsuite.exe

# Git Bash crash artifacts
*.stackdump

# internal planning docs - kept local, not published
docs/superpowers/
.lavish/
